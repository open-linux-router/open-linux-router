Keep a public name pointing at this router, updating it whenever the
address changes.

Two things have to be said: whose DNS the name lives in, and where the
address comes from. The second is the one to get right.

  --reflector <url>   ask an endpoint what address the internet sees.
                      Right when olr sits behind a modem, which is the
                      usual setup, and the only form that can tell you
                      your ISP has put you behind carrier-grade NAT.
  --interface <name>  read the address off an uplink this router owns.
                      Right when olr terminates the WAN itself — PPPoE,
                      or an address from the ISP by DHCP.

olr will not choose between them. One of them makes this box talk to a
third party every few minutes and the other does not, and that is not a
decision to make on your behalf.

Adding a name does not expose anything. A current DNS record and an
open port are two separate decisions; `olr firewall` is the other one.

Examples:
  olr dial add home.example.net --provider cloudflare \
      --token-file - --reflector https://api.ipify.org
  olr dial add home.example.net --provider alidns \
      --key-id LTAI... --token-file key.txt --interface ppp0

Usage:
  olr dial add <name> [flags]

Flags:
  -h, --help                help for add
      --interface string    the uplink: the interface whose address is published, or the one a reflector is asked through
      --interval string     how often to check the address, with a unit (default 5m0s)
      --key-id string       the non-secret half of the credential: an AccessKey ID at Alibaba Cloud, a SecretId at Tencent Cloud
      --no-reflector        read the address off the interface instead of asking an endpoint
      --provider string     DNS provider hosting the name (see olr dial show providers)
      --reflector string    ask this endpoint what address the internet sees, instead of reading the interface
      --token string        the provider credential. Prefer --token-file: a value here is recorded in your shell history
      --token-file string   read the provider credential from a file, or from standard input when given -
      --ttl int             the published record's time to live in seconds (default the provider's own)
      --url string          for the callback provider: the URL to request, with #{ip} where the address goes
      --zone string         registered domain the name sits in, when olr cannot work it out from the name itself

Global Flags:
      --dry-run         show what would change and exit without applying it
  -o, --output string   output format: text|json (default "text")
      --socket string   olrd control socket (default "/run/olr/olrd.sock")
